import React from 'react';
import { Container } from 'react-bootstrap';

const Contacts = () => {
    return (
        <Container>

            <Contacts/>
            
        </Container>
    );
};

export default Contacts;